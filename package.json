{
  "name": "community-assistant",
  "private": true,
  "version": "0.1.0",
  "description": "Community AI Assistant monorepo root (tooling only)",
  "scripts": {
    "prepare": "husky",
    "commitlint": "commitlint --edit"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "husky": "^9.1.7"
  },
  "engines": {
    "node": ">=20"
  }
}
