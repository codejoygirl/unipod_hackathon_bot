import { PlaceholderScreen } from "@/components/chat/placeholder-screen";

export default function MeetingsPage() {
  return (
    <PlaceholderScreen
      title="Meetings"
      description="Meeting notes and schedules are not wired in the web app yet. This tab keeps the mobile shell ready for a later phase."
    />
  );
}
