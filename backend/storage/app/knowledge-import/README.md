# Staged knowledge imports

WhatsApp `_chat.txt` exports for:

```bash
php artisan zak:import-knowledge-chats
```

**Temporary in git** so production can `git pull` and import without `scp`. Contains community chat content (PII). Delete from the repo when you switch to private staging only.
